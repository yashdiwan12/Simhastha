services:
  - type: web
    name: simhastha-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python seed_database.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
